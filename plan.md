# pjsk_pic WebUI 审核流程计划

## 当前阶段

- 1期：优化 Pixiv 审核页的人工决策流程。（已完成）
- 2期：修复大图预览新增主 tag 的表单定位问题。（已完成）
- 3期：移除单张拒绝图片的二次确认。（已完成）
- 4期：Pixiv 审批改为规范主 tag + alias / 搜索词沉淀闭环。（已完成）
- 5期：Pixiv 审批页增加角色/alias 搜索并重整筛选与批量审核交互。（已完成）
- 6期：独立 WebUI 从内嵌 HTML/JS 重构为 Vue 3 + Vite + TypeScript 工作台。（已完成）

## 总目标

- 将“通过某个主 tag”和“整张 Pixiv 图不要收”区分为两条明确路径。
- 审核通过时支持直接新增主 tag，并在选择来源 tag 时沉淀 Pixiv 来源词映射。
- 审核通过时只把图片归入一个规范主 tag，其他同义词沉淀为 alias / Pixiv 搜索词。
- 审核拒绝整张图时记录 Pixiv 来源跳过信息，避免后续自动来源搜图再次入队。
- 审批前先用角色、alias 或 Pixiv tag 搜到相关待审图，再进行单张或批量审核。
- WebUI 采用独立前端工程维护，降低后续复杂交互迭代成本。

## 阶段拆分

### 1期：Pixiv 审核页通过/拒绝闭环（已完成）

- 在候选主 tag 区新增手动添加入口。
- 通过审核时写入主 tag 关系，并把选中的 Pixiv 来源 tag 映射到主 tag。
- 在审批页新增“拒绝图片”动作，记录图片级 Pixiv 拒绝来源。
- 自动来源搜图跳过已拒绝的 Pixiv 来源。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 2期：新增主 tag 弹窗热修（已完成）

- 修复卡片区和大图预览同时渲染新增主 tag 表单时的 id 冲突。
- 点击大图预览里的“添加”时，只读取当前预览表单内的输入值。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 3期：拒绝图片交互简化（已完成）

- 单张“拒绝图片”点击后直接调用拒绝接口，不再弹出浏览器确认框。
- 保留批量审核、平台词删除、tag 归并等其他高风险操作的确认框。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 4期：规范主 tag 与 alias / 搜索词闭环（已完成）

- Pixiv 审批页候选 tag 改为“归入主 tag”单选。
- Pixiv 来源 tag 改为 alias / Pixiv 搜索词多选。
- 审核提交时只给图片写入规范主 tag，所选同义词沉淀为 alias / Pixiv 搜索词。
- 如果同义词已经是独立 tag，则通过现有 tag 合并能力归并到规范主 tag。
- 自动来源搜图继续使用规范主 tag 下的 alias / Pixiv 搜索词搜索与匹配。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 5期：角色搜索与审核交互重整（已完成）

- Pixiv 审批页顶部改为筛选区，支持按角色、alias、Pixiv tag 搜索待审图。
- 搜索命中主 tag 后会展开 alias、Pixiv 平台词，以及历史来源中出现过的同义角色候选。
- 批量审核区从筛选区中拆出，默认收起，勾选图片后再突出显示批量归入和 alias 操作。
- 空搜索保持原有待处理队列，搜索为空结果时提示可补 alias / Pixiv 平台词。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 6期：Vue 3 WebUI 框架化重构（已完成）

- 新增 `webui/` 前端工程，使用 Vue 3、Vite、TypeScript 和 hash 路由承载 8 个 WebUI 页面。
- `core/webui.py` 保留现有 aiohttp API，并优先返回构建后的 Vue 静态入口。
- 构建产物随插件仓库发布到 `core/webui_static/`，普通用户安装插件不需要 Node/npm。
- 按工作台式管理界面重整导航、筛选、列表、预览弹窗、toast、loading 和批量操作反馈。
- 更新 README 和版本号，完成 runtime 热重载与插件仓库提交推送。

### 后续可选

- 给拒绝来源增加恢复入口。
- 给拒绝原因增加结构化分类，例如 R18、AI、质量不足、非目标角色。
- 给审核页补充按拒绝来源查看和批量恢复能力。
